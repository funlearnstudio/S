# SE Language for VS Code

Supports `.se` files with syntax highlighting, indentation, snippets, and terminal-backed Run/Check/Build commands. The extension deliberately calls the installed `se` CLI instead of implementing a second parser, so editor commands use the real SE compiler/runtime.
