# time

`time` provides time values, while duration literals are part of the language.

```text
use time

now = time.now
say now
wait 500ms
wait 2s
```

S 0.2 duration suffixes are `ms`, `s`, and `min`.
