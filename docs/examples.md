# Examples

Every `.s` file in `examples/` is executed by the project test workflow. The directory covers output, input, arithmetic, branches, every 0.1 loop, lists, functions, and the full greeting program.

Try:

```bash
./build/s run examples/loop.s
./build/s build examples/function.s
./examples/function
```
